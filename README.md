# Starpets Marketplace

Тестовое задание для starpets.gg
## 🛠 Используемые инструменты

### Frontend Framework
- **Nuxt 4.2.1** 
- **Vue 3.5.25** 
- **Vue Router 4.6.3** 

### Управление состоянием
- **Pinia 3.0.4** 
- **@pinia/nuxt 0.11.3**
- **@tanstack/vue-query 5.92.0** 

### Стилизация
- **Sass 1.94.2**
- **Sass-loader 16.0.6**
- **БЭМ методология**

### Линтинг и форматирование
- **ESLint 9.39.1**
- **@nuxt/eslint 1.10.0**
- **Prettier 3.4.2**

### TypeScript
- **TypeScript 5.9.3**
- **vue-tsc 3.1.5**

### Дополнительные инструменты
- **vite-svg-loader 5.1.0**
- **@nuxtjs/google-fonts 3.2.0**

## 🏗 Архитектура

Проект построен на методологии **Feature-Sliced Design (FSD)**

### Структура слоев

```
src/
├── app/           # Инициализация приложения, глобальные настройки
├── pages/         # Страницы приложения (роуты)
├── widgets/       # Композитные блоки интерфейса
├── features/      # Функциональные модули (пользовательские сценарии)
├── entities/      # Бизнес-сущности приложения
└── shared/        # Переиспользуемый код без бизнес-логики
```

### Слои и их назначение

#### 📱 **app/** - Слой приложения
Инициализация приложения, глобальные провайдеры и конфигурация.
- `layouts/` - Layout компоненты
- `plugins/` - Плагины Nuxt (TanStack Query)
- `model/` - Глобальные stores (UI состояние)

#### 📄 **pages/** - Страницы
Страницы-роуты приложения:
- `index.vue` - Главная страница (маркет)
- `products.vue` - Каталог продуктов
- `profile.vue` - Профиль пользователя
- `help.vue`, `sale.vue`, `rbx.vue` - Дополнительные страницы

#### 🧩 **widgets/** - Виджеты
Сложные составные компоненты интерфейса:
- `header/` - Шапка приложения
- `navigation/` - Навигационные меню
- `recent-purchases/` - Виджет последних покупок

#### ⚙️ **features/** - Фичи
Законченные пользовательские функции:
- `add-to-cart/` - Добавление товаров в корзину
- `products-filter/` - Фильтрация продуктов
- `search-history/` - История поиска

#### 📦 **entities/** - Сущности
Основные бизнес-сущности:
- `product/` - Продукты (api, model, ui)
- `user/` - Пользователь (api, model)

#### 🔧 **shared/** - Общий код
Переиспользуемые модули без привязки к бизнес-логике:
- `api/` - HTTP клиент
- `assets/` - Изображения, стили, SVG
- `config/` - Конфигурация
- `lib/` - Утилиты и хелперы
- `types/` - TypeScript интерфейсы и enums
- `ui/` - UI компоненты

### Принципы FSD

**Правило импортов:**
Слои могут импортировать только нижележащие слои:
```
app → widgets → features → entities → shared
```

**Публичный API:**
Каждый модуль экспортирует свой публичный API через `index.ts`

**Сегментация:**
Внутри слайса используется стандартная структура: `api/`, `model/`, `ui/`, `lib/`, `config/`

## 🔄 TanStack Query - Управление серверным состоянием

В проекте используется **@tanstack/vue-query** (ранее Vue Query) для управления асинхронным состоянием, кешированием и синхронизацией серверных данных.

### Конфигурация

В проекте TanStack Query настроен как Nuxt плагин в `src/app/plugins/tanstack-query.client.ts`:

```typescript
const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 1000 * 60 * 5,        // Данные "свежие" 5 минут
      gcTime: 1000 * 60 * 10,          // Кеш хранится 10 минут
      refetchOnWindowFocus: false,      // Не обновлять при фокусе окна
      refetchOnMount: true,             // Обновлять при монтировании
      refetchOnReconnect: true,         // Обновлять при восстановлении сети
      retry: 2,                         // 2 попытки при ошибке
      retryDelay: (attemptIndex) =>     // Экспоненциальная задержка
        Math.min(1000 * 2 ** attemptIndex, 1000),
    },
    mutations: {
      retry: false,                     // Не повторять мутации
    },
  },
});
```

**Ключевые параметры:**
- **staleTime (5 мин)** - время, в течение которого данные считаются актуальными
- **gcTime (10 мин)** - время хранения неиспользуемых данных в кеше
- **retry (2 попытки)** - автоматическая повторная попытка при ошибке с экспоненциальной задержкой

### Как это работает в проекте

#### 1. API слой (`entities/product/api/`)

Создаем переиспользуемые query hooks:

```typescript
// entities/product/api/useProducts.ts
export function useProductQuery(
  queryParams: MaybeRefOrGetter<FetchProductsRequest>,
  syncToStore?: (response: FetchProductsResponse) => void,
) {
  const query = useQuery({
    queryKey: ['product-query', queryParams],  // Уникальный ключ для кеша
    queryFn: () => productApi.fetchProducts(queryParams),
    enabled: import.meta.client,               // Только на клиенте
  });

  // Опциональная синхронизация с Pinia store
  if (syncToStore) {
    watch(() => query.data.value, (data) => {
      if (data) syncToStore(data);
    });
  }

  return query; // { data, isLoading, error, refetch, ... }
}
```

#### 2. Использование в компонентах

```typescript
// В компоненте или странице
const { data, isLoading, error, refetch } = useProductQuery(
  computed(() => ({
    search: searchQuery.value,
    filters: activeFilters.value,
  })),
  (response) => {
    // Синхронизация с Pinia store
    productsStore.setProducts(response.items);
  }
);
```

### Примеры использования в проекте

- **Загрузка продуктов** - `useProductQuery` для получения списка товаров с фильтрами
- **Реактивные параметры** - фильтры и поиск автоматически триггерят новые запросы
- **Синхронизация** - данные из TanStack Query синхронизируются с Pinia stores

## 🚀 Как запустить проект

### Требования
- Node.js >= 18.x
- npm или yarn

### Установка зависимостей

```bash
yarn install
```

### Запуск в режиме разработки

```bash
yarn run dev
```

Приложение будет доступно по адресу: `http://localhost:3333`

### Другие команды разработки

```bash
# Проверка типов TypeScript
yarn run typecheck

# Проверка линтера
yarn run lint:check

# Исправление ошибок линтера
yarn run lint

# Проверка форматирования
yarn run format:check

# Автоформатирование кода
yarn run format
```

## 📦 Как собрать проект

### Production сборка

```bash
yarn run build
```

Результат сборки будет находиться в директории `.output/`

### Предпросмотр production сборки

```bash
yarn run preview
```

### Генерация статического сайта (SSG)

```bash
yarn run generate
```

Статические файлы будут сгенерированы в директории `.output/public/`

## ⚙️ Переменные окружения

Создайте файл `.env` в корне проекта:

```env
API_URL=https://your-api-url.com
```

## 📝 TODO - Планы по улучшению

> **Примечание:** Проект сверстан быстро (~16 часов работы), есть косяки, сделано без pixel perfect.

Что необходимо доработать при наличии дополнительного времени:

### 1. 🎨 Оптимизация ассетов
- [ ] Перевести все иконки на SVG sprite для улучшения производительности
- [ ] Оптимизировать загрузку изображений

### 2. 🧪 Тестирование
- [ ] Написать Unit тесты для бизнес-логики (stores, utils)
- [ ] Написать интеграционные тесты для компонентов
- [ ] Настроить E2E тестирование

### 3. 🎨 Рефакторинг стилей
- [ ] Отрефакторить стили (часть стилей делалась с помощью ИИ для ускорения)
- [ ] Привести все стили к единому стандарту БЭМ
- [ ] Оптимизировать повторяющиеся стили

### 4. ♾️ UX улучшения
- [ ] Реализовать бесконечный скролл для списка продуктов (в задаче не было явно указано)
- [ ] Добавить skeleton loaders для лучшего UX при загрузке
- [ ] Улучшить обработку ошибок и показ уведомлений

### 5. 🎴 Компоненты карточек
- [ ] Переработать карточки продуктов - вынести из обычных кнопок в отдельные UICard компоненты
- [ ] Добавить состояния загрузки (loaders) для карточек
- [ ] Унифицировать логику всех типов карточек

### 6. 🐳 Docker
- [ ] Написать Dockerfile для контейнеризации приложения
- [ ] Создать docker-compose для локальной разработки
- [ ] Добавить инструкции по развертыванию через Docker

### 7. 🔧 Технический долг
- [ ] Рефакторинг скриптов
- [ ] Расширить типизацию (сейчас более черновой вариант)
- [ ] Оптимизировать bundle size
- [ ] Добавить pre-commit хуки для линтинга и тестов

## 📄 Дополнительная информация

### Стайлгайд проекта

- **Функции:** Только Function Declaration (не стрелочные функции)
- **Стили:** Строго БЭМ методология
- **TypeScript:**
- **Принципы:** SOLID и KISS

### Структура компонентов

Все компоненты автоматически импортируются благодаря настройке в `nuxt.config.ts`:
- UI компоненты с префиксом `UI` (UIActionButton, UIModalContainer)
- Entity компоненты без префикса (MarketProductCard, UserAvatar)
- Widget компоненты без префикса (Header, MobileBottomNavigation)

---

**Автор:** Дмитриев
